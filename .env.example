# Example env vars (NO real secrets). Copy to .env and fill in; .env is git-ignored.
# Every key is OPTIONAL — the assistant runs fully without any of them (it degrades
# to stored data / great-circle distance / nearest-city only).
NEON_DATABASE_URL=
CLIMATIQ_API_KEY=
AVIATIONSTACK_API_KEY=
OPENCAGE_API_KEY=
OPENROUTESERVICE_API_KEY=
