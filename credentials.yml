# Input channels for the Eco-Travel Advisor.
# The REST channel exposes POST /webhooks/rest/webhook, which the frontend
# (frontend/app.js, MODE = "rasa") calls. An empty "rest:" entry is all that is
# needed to enable it.
rest:

# The action server endpoint is configured in endpoints.yml, not here.
